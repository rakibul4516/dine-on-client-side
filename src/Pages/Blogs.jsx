import { Helmet } from "react-helmet";

const Blogs = () => {
    return (
        <div>
            <Helmet>
                <title>Blogs page</title>
            </Helmet>
            <div>
                this is blog page
            </div>
        </div>
    );
};

export default Blogs;